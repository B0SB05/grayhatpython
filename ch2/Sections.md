#2.1 General-Purpose CPU Registers 

The following are registers we will understand them : EAX EDX ECX ESP EBP EDI ESI EBX

EAX : accumulator Registery --> Used to for performing calculations as well as storing return values from function calls. 

You can easily determine if a function call has faild or succeeded based on the value stored in EAX. In addition, you can determin the actual value of what the function is returning   

